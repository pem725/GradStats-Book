# Python packages the book's Python tabs import.
#
# _engines.R runs those tabs with .venv/bin/python, so this file describes the
# environment the printed Python output was actually produced in. The versions
# are the ones the book was last rendered and checked against; the build server
# installs exactly these so the numbers on the site match the numbers here.
#
# To build it yourself:
#     python -m venv .venv
#     .venv/bin/python -m pip install -r requirements.txt
#
# This list is reprinted in the "Setup and Required Packages" appendix.

pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
matplotlib==3.11.1
statsmodels==0.14.6
patsy==1.0.2
scikit-learn==1.9.0
plotly==6.9.0
