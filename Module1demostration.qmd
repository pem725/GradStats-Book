---
title: "Module 1: Getting Started"
---



```{r setup}
library(psych)
data(mtcars)
describe(mtcars)
hist(mtcars$mpg)
hist(mtcars$hp)

```


```{r}
mpg.z <- scale(mtcars$mpg)

hist(mpg.z)
hist(mtcars$mpg)

```

